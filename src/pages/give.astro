---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Give · Shipyard Church" description="Support the work of Shipyard Church. Your generosity goes directly toward caring for those in need.">
  <Header activePage="give" />

  <div class="page-header">
    <div class="page-header-inner">
      <h1 class="page-title">Give</h1>
      <p class="page-subtitle">Supporting the work of the church</p>
    </div>
  </div>

  <main class="content">
    <p class="intro">
      Shipyard operates without paid staff. <strong>Your generosity goes directly toward caring for those in need</strong>, supporting gatherings, and equipping disciples to go and multiply. Thank you for your partnership.
    </p>

    <div class="giving-card">
      <div class="giving-icon">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
        </svg>
      </div>
      <h2 class="giving-title">Secure Online Giving</h2>
      <p class="giving-text">Give securely through PushPay. You can make a one-time gift or set up recurring giving.</p>
      <a href="https://pushpay.com/g/shipyardchurchduxbury" class="giving-btn" target="_blank" rel="noopener">
        Give Now
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="5" y1="12" x2="19" y2="12"></line>
          <polyline points="12 5 19 12 12 19"></polyline>
        </svg>
      </a>
      <p class="giving-secure">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
          <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
        </svg>
        Secure, encrypted transaction
      </p>
    </div>
  </main>

  <Footer />
</Layout>

<style>
  .content {
    max-width: 640px;
    margin: 0 auto;
    padding: 4rem 2rem;
  }

  .intro {
    font-size: 1.1rem;
    color: var(--text-secondary);
    line-height: 1.8;
    margin-bottom: 2.5rem;
    text-align: center;
  }

  .intro strong {
    color: var(--warm-black);
    font-weight: 500;
  }

  .giving-card {
    background: var(--white);
    border-radius: 16px;
    padding: 3rem;
    box-shadow: 0 1px 3px rgba(61, 54, 48, 0.04);
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .giving-card::before {
    content: '';
    position: absolute;
    top: -40px;
    right: -40px;
    width: 120px;
    height: 120px;
    border: 1px solid var(--border);
    border-radius: 50%;
    opacity: 0.5;
  }

  .giving-card::after {
    content: '';
    position: absolute;
    bottom: -30px;
    left: -30px;
    width: 80px;
    height: 80px;
    border: 1px solid var(--border);
    border-radius: 50%;
    opacity: 0.3;
  }

  .giving-icon {
    width: 56px;
    height: 56px;
    background: var(--cream);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
    position: relative;
  }

  .giving-icon svg {
    width: 28px;
    height: 28px;
    color: var(--olive);
  }

  .giving-title {
    font-family: 'Lora', Georgia, serif;
    font-size: 1.35rem;
    font-weight: 500;
    color: var(--warm-black);
    margin-bottom: 0.75rem;
    position: relative;
  }

  .giving-text {
    font-size: 0.95rem;
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 2rem;
    position: relative;
  }

  .giving-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.625rem;
    background: var(--olive);
    color: var(--white);
    font-weight: 600;
    font-size: 1rem;
    padding: 1rem 2rem;
    border-radius: 8px;
    text-decoration: none;
    transition: background 0.2s ease, transform 0.15s ease, box-shadow 0.2s ease;
    position: relative;
  }

  .giving-btn:hover {
    background: var(--olive-dark);
    transform: translateY(-2px);
    box-shadow: 0 4px 16px rgba(92, 107, 74, 0.25);
  }

  .giving-btn svg {
    width: 18px;
    height: 18px;
  }

  .giving-secure {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.375rem;
    margin-top: 1.25rem;
    font-size: 0.8rem;
    color: var(--text-muted);
    position: relative;
  }

  .giving-secure svg {
    width: 14px;
    height: 14px;
  }

  @media (max-width: 900px) {
    .content {
      padding: 2.5rem 1.5rem;
    }

    .intro {
      font-size: 1.05rem;
    }

    .giving-card {
      padding: 2.5rem 2rem;
    }
  }
</style>
